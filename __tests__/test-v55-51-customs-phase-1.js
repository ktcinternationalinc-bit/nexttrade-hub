// v55.83-A.4 — RETIRED (Max May 13 2026)
// This suite tested supabase/customs-phase-1.sql (the v55.51 customs broker
// initial schema). That feature was redesigned and the SQL file removed
// from the repo. Current customs functionality lives in the Customs tab
// component (CustomsTab.jsx); there's no separate SQL file to verify.
//
// Reason for retirement: file no longer exists; the test cannot proceed.
console.log('⚠️  test-v55-51-customs-phase-1 RETIRED — feature rearchitected');
process.exit(0);
