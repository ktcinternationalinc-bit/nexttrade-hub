{
  "name": "living-companion-server",
  "version": "0.1.0",
  "description": "Real-time voice orchestrator for the KTC NextTrade Living Avatar system. Routes mic → Deepgram → Claude → ElevenLabs → client over Socket.io with barge-in support.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "@deepgram/sdk": "^3.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "socket.io": "^4.7.5",
    "ws": "^8.18.0"
  }
}
